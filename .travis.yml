sudo: false
language: python
git:
    depth: 200
matrix:
  include:
    - env: TOXENV=py27
      addons:
        apt:
          packages:
           - python-dev
           - libyaml-dev
    - env: TOXENV=py3
      addons:
        apt:
          packages:
           - python3-dev
           - libyaml-dev
install: pip install tox
script:
    - tox -e $TOXENV
